import conexao from "../../config/db.js";

const modelProduto = {
    cadastrar: async () => {
        try {
        } 
        catch (erro) {
            throw erro;
        }
    },

}

export default modelProduto;
