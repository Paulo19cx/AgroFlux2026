import conexao from "../../config/db.js";

const modelProduto = {
    cadastrar: async () => {
        try {
            const resultado
        } 
        catch (erro) {
            throw erro;
        }
    },

}

export default modelProduto;